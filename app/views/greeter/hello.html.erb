<b><u>RRI-3.03.1</u></b>
<h1>Greeter#hello</h1>
<p>Find me in app/views/greeter/hello.html/erb.</p>

<br/><br/><br/>
<b><u>RRI-3.03.2</u></b>
<% random_names = ["Alex", "Joe", "Michael"] %>
<h1>Greetings, <%= random_names.sample %></h1>
<p>The time is now <%= Time.now %></p>
<!-- ^ This is bad bcz we don't want (business) logic in the view, or at least
		we want to minimize the amount of Ruby logic that's in any view.  -->

<br/><br/><br/>
<b><u>RRI-3.05.1</u></b>
<h1>Greetings, <%= @name %></h1>
<p>The time is now <%= @time %></p>
<p>This page has been viewed <%= @times_displayed %> times(s)</p>
<!-- It's still fine to have Ruby code embedded, but the declaration and
	similar has been moved to the Controller. -->

<br/><br/><br/>
<b><u>RRI-3.06.1</u></b>
<h1>Greetings, <%= @name %></h1>
<p>The time is now <%= formatted_time(@time) %></p>
<!-- Here's an example of our helper ^^^ -->
<p>This page has been viewed <%= @times_displayed %> times(s)</p>
<!-- link-to is an example of a built-in helper -->
<!-- Note that link-to is a method, so my Python-esque syntax will work too: -->
<p><%= link_to("Google", "http://www.google.com") %></p>
<p><%= link_to("Goodbye", greeter_goodbye_path) %></p>
<br/><br/><br/>

